import 'package:flutter/material.dart';

class BlogFragment extends StatelessWidget {
  const BlogFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Blog')
      ),
    );
  }
}
